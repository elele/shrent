<%= form_for([:admin,@admin_house]) do |f| %>
  <% if @admin_house.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_house.errors.count, "error") %> prohibited this admin_house from being saved:</h2>

      <ul>
      <% @admin_house.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
