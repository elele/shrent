<!DOCTYPE html>
<html>
<head>
  <title>Shrent</title>
  <%= stylesheet_link_tag    "boot", :media => "all" %>
  <%#= javascript_include_tag "" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div id="container">  
    <div class="user_nav">  
      <% if current_user %>  
        登录用户: <%= current_user.username %>.  
        <%= link_to "注销", logout_path %>  
      <% else %>  
        <%= link_to "注册", signup_path %> 或 
        <%= link_to "登录", login_path  %>.  
      <% end %>  
    </div>  
    <% flash.each do |name, msg| %>  
      <%= content_tag :div, msg, :id => "flash_#{name}" %>  
    <% end %>  
    <%= yield %>  
  </div>  
</body>
</html>
